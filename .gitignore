# 依赖
__pycache__/
*.py[cod]
.venv/
venv/
env/

# 前端
node_modules/
frontend/dist/
*.local

# 打包/构建产物与本地缓存（体积大，不入库）
build/
build-temp/
dist/
build/                     # 兼容旧配置
backend-dist/
electron-dist/
.npm-cache/
_asar-extract/
_asar-repack/
.tmp-asar-check/
# PyInstaller 打包配置是源码，需入库（覆盖上面的 *.spec 忽略）
*.spec
!build.spec

# 应用运行数据
*.db
*.sqlite3
config.json
.appdata/
data/
music/
fonts/
# 在线播放音频缓存（可随时清空，不入库）
cache/
# 自定义源脚本（第三方代码，绝不入库；开发模式下落在 <项目根>/sources/）
/sources/

# AI 助手项目记忆（本地工作数据，不入库）
.workbuddy-ai/

# 本地专用脚本（不入库）：依赖第三方源样本、或需手工指定源文件的调试工具。
# 可自动化的测试已迁移到 tests/（入库，由 CI 运行）。
electron/__test-*.js
app/__test-*.py

# 编辑器 / 系统
.idea/
.vscode/
.DS_Store
Thumbs.db
